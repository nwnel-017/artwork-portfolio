export type Collection = {
  title: string;
};

export type CollectionCard = {
  id: string;
  collection_name: string;
  image_path: string;
};

export type CollectionDetails = {
  collection_name: string;
  desc: string;
};
