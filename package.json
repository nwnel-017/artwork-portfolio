{
  "name": "artwork-portfolio",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "types": "supabase gen types typescript --project-id nlsielazfhfgodwsmhhz --schema public > ./types/supabase/database.ts",
    "webhook": "stripe listen --forward-to http://localhost:3000/api/stripe/webhook"
  },
  "dependencies": {
    "@nuxtjs/supabase": "^2.0.3",
    "@stripe/stripe-js": "^8.6.1",
    "@supabase/supabase-js": "^2.90.1",
    "nuxt": "^4.2.2",
    "stripe": "^20.1.2",
    "vue": "^3.5.26",
    "vue-router": "^4.6.4",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
